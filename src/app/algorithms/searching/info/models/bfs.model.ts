import { SearchingAlogoritm } from './searching.models';

export class BreadthFirstSearch<T> implements SearchingAlogoritm<T> {
  traverse(tree: any) {
    throw new Error("Method not implemented.");
  }
}